{
  "name": "Officer",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Scyllinice",
  "license": "MIT",
  "scripts": {
    "install": "prebuild-install || cmake-js compile",
    "rebuild": "prebuild --backend cmake-js --compile",
    "prebuild": "prebuild --backend cmake-js -r electron -t 4.0.0 --verbose",
    "test": "mocha tests"
  },
  "dependencies": {
    "bindings": "^1.3.0",
    "boost-lib": "https://github.com/GameCommandeer/boost-lib.git",
    "cmake-js": "^4.0.1",
    "nan": "^2.11.1",
    "prebuild-install": "^5.2.1"
  },
  "devDependencies": {
    "electron-rebuild": "^1.8.2",
    "mocha": "^5.2.0",
    "prebuild": "^8.1.1"
  }
}
